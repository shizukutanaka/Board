# ADR-0389: 同一トーストの再付け替え (スタック増殖防止)

## 状態
採用 (v1.7.431)

## 背景
`_tst` は呼ぶたびに div を append — 同一メッセージの連発 (キー長押し・
繰り返し拒否) でトーストスタックが縦に並び続ける視覚ノイズだった。

## 決定
直前のトーストと同文なら remove して再 append — スタックは増えず、
aria-live の DOM 追加として再通知も維持 (SR parity 継続)。

## 断念した代替案
- 同一文を完全スキップ: aria-live の再通知も失われるため不採用
